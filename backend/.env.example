PORT=8080
NODE_ENV=development

# m2 阶段使用 mock 登录和 mock 数据
AUTH_MODE=mock
DATA_MODE=mock

# 本地开发用。生产环境必须显式配置，不要依赖默认值。
JWT_SECRET=dev-only-secret

# 本地前端地址。多个 origin 用逗号分隔。
CORS_ORIGIN=http://localhost,http://localhost:80,http://localhost:5173

LOG_PRETTY=true